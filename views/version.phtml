<?php

/** @var \App\Controller\VersionController $this */
/** @var \App\View\VersionView $view */
$currentPage = 'version';

$itemApiEndpoint = '/api/jira/projects/{key}/versions';
$formAction      = '/version';
$inputName       = 'fixVersionId';
$storageKey      = 'jira_project_version';
?>
<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <title>VSM - Version Jira</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/style.css">
</head>

<body>
    <?php include __DIR__ . '/components/topNavigation.phtml'; ?>

    <div class="card">
        <?php include __DIR__ . '/components/form/selectJiraProject.phtml'; ?>

        <form method="POST" action="/version">
            <label><em>Ou directement par FixVersion ID :</em></label>
            <input type="text" name="fixVersionId" required placeholder="ID de la Version">
            <button type="submit">OK</button>
        </form>
    </div>

    <?php include __DIR__ . '/components/errorMessage.phtml'; ?>

    <?php if ($view): ?>
        <div class="card">
            <h2>
                <a href="<?= $view->getVersionUrl(); ?>" target="_blank">
                    <?= $view->getVersionName(); ?> (<?= 'ID ' . $view->getVersionId(); ?>)
                </a>
            </h2>

            <p style="margin-top: 12px; color: var(--muted);">
                <?= $view->getVersionDescription(); ?>
            </p>

            <div class="meta">
                <!-- Infos version -->
                <div class="meta-line meta-line-info">
                    <span>📅 Date de démarrage : <?= $view->getVersionStartDate() ?? '—'; ?></span>
                    <span>🎯 Date cible de livraison : <?= $view->getVersionReleaseDate() ?? '—'; ?></span>

                    <?php if ($view->isVersionOverdue()): ?>
                        <span class="badge red">🕗Deadline dépassée</span>
                    <?php endif; ?>

                    <?php if ($view->isVersionReleased()): ?>
                        <span class="badge green">Statut : Terminée 🚀</span>
                    <?php else: ?>
                        <span class="badge orange">Statut : En cours</span>
                    <?php endif; ?>
                </div>

                <?php include __DIR__ . '/components/metrics/totals.phtml'; ?>
            </div>
        </div>

        <div class="timeline-grid">
            <!-- Timeline par statut -->
            <?php include __DIR__ . '/components/metrics/timelineChart.phtml'; ?>
        </div>

        <div class="card">
            <h2><?= $view->getIssuesCount(); ?> tickets rattachés à cette version :</h2>
            <?php include __DIR__ . '/components/issuesList.phtml'; ?>
        </div>

    <?php endif; ?>

</body>

</html>